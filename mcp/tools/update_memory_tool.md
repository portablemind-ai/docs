<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# update_memory_tool

> Correct or refine an existing memory you own (title / content / importance / tags).

| | |
|---|---|
| Category | `memory` |
| Callable | MCP clients; in-app @ai conversations (always on); grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=update_memory_tool` |

## Description

This is the description the server sends to the model, verbatim.

Correct or refine an existing memory you own (title / content / importance / tags).

USE WHEN: a fact you stored is now wrong or incomplete and should be updated in place
(rather than storing a contradicting memory).

REQUIRED: memory_id
OPTIONAL: title, content, importance_score, tags [], llm_agent_id (caller identity for ownership)

You can only update memories you created. To retire a memory entirely, use forget_memory.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `memory_id` | integer | yes | ID of the memory to update (required) |
| `title` | string | no | New title (optional) |
| `content` | string | no | New content body (optional, replaces existing content) |
| `importance_score` | number | no | New importance 0.0-1.0 (optional) |
| `tags` | array of string | no | New tags (optional, replaces existing) |
| `llm_agent_id` | integer | no | Calling agent id (used to verify ownership) |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "memory_id": {
      "type": "integer",
      "description": "ID of the memory to update (required)"
    },
    "title": {
      "type": "string",
      "description": "New title (optional)"
    },
    "content": {
      "type": "string",
      "description": "New content body (optional, replaces existing content)"
    },
    "importance_score": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "New importance 0.0-1.0 (optional)"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "New tags (optional, replaces existing)"
    },
    "llm_agent_id": {
      "type": "integer",
      "description": "Calling agent id (used to verify ownership)"
    }
  },
  "required": [
    "memory_id"
  ]
}
```

Back to the [tool index](../README.md).