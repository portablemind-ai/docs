<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# agent_scorecard_tool

> Agent Report Cards: A–F grades per agent computed nightly from the AG-UI audit log (tool failures, run errors, wasted calls, silent runs) and from what the h...

| | |
|---|---|
| Category | `data`, `admin` |
| Callable | MCP clients; in-app @ai conversations; grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=agent_scorecard_tool` |

## Description

This is the description the server sends to the model, verbatim.

Agent Report Cards: A–F grades per agent computed nightly from the AG-UI audit log (tool failures, run errors, wasted calls, silent runs) and from what the humans said next (rework rate). fleet = grade table for every active agent; agent = one agent's full card, history and evidence pack (worst tools, correction excerpts); recompute = rebuild one agent's cards now; override = a human overrules a deliverable's label. Grades are per prompt version too (by_prompt_digest), so a prompt edit reads as an experiment. Platform errors and cost-cap halts never count against an agent.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `tenant_id` | string | yes | Tenant ID or enterprise identifier. |
| `action` | string | no | fleet (default) \| agent \| recompute \| override. One of: `fleet`, `agent`, `recompute`, `override`. |
| `llm_agent_id` | integer | no | Agent id — required for agent / recompute. |
| `window_days` | integer | no | Card window: 30 (default) or 7. |
| `history_limit` | integer | no | agent: how many prior cards to return (default 12) |
| `agent_message_id` | integer | no | override: the agent message (deliverable) to relabel. |
| `label` | string | no | override: accepted \| correction \| clarification \| new_request \| unrelated \| none. One of: `accepted`, `correction`, `clarification`, `new_request`, `unrelated`, `none`. |
| `note` | string | no | override: why (stored as rationale) |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "tenant_id": {
      "type": "string",
      "description": "Tenant ID or enterprise identifier"
    },
    "action": {
      "type": "string",
      "enum": [
        "fleet",
        "agent",
        "recompute",
        "override"
      ],
      "description": "fleet (default) | agent | recompute | override"
    },
    "llm_agent_id": {
      "type": "integer",
      "description": "Agent id — required for agent / recompute"
    },
    "window_days": {
      "type": "integer",
      "description": "Card window: 30 (default) or 7"
    },
    "history_limit": {
      "type": "integer",
      "description": "agent: how many prior cards to return (default 12)"
    },
    "agent_message_id": {
      "type": "integer",
      "description": "override: the agent message (deliverable) to relabel"
    },
    "label": {
      "type": "string",
      "enum": [
        "accepted",
        "correction",
        "clarification",
        "new_request",
        "unrelated",
        "none"
      ],
      "description": "override: accepted | correction | clarification | new_request | unrelated | none"
    },
    "note": {
      "type": "string",
      "description": "override: why (stored as rationale)"
    }
  },
  "required": [
    "tenant_id"
  ]
}
```

Back to the [tool index](../README.md).