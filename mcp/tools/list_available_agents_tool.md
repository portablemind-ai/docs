<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# list_available_agents_tool

> List available agents and what they can do, it will return a JSON array of Objects containing the name: (name of the agent to be used when calling it) and de...

| | |
|---|---|
| Category | `agents` |
| Callable | MCP clients; in-app @ai conversations (default) |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=list_available_agents_tool` |

## Description

This is the description the server sends to the model, verbatim.

List available agents and what they can do, it will return a JSON array of Objects containing the name: (name of the agent to be used when calling it)
and description: (what the agent specializes in and should be used for)'

## Parameters

This tool takes no parameters.

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {},
  "required": []
}
```

Back to the [tool index](../README.md).