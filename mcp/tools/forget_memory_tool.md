<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# forget_memory_tool

> Retire a memory you own (soft-archive — recoverable, not permanently deleted).

| | |
|---|---|
| Category | `memory` |
| Callable | MCP clients; in-app @ai conversations (always on); grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=forget_memory_tool` |

## Description

This is the description the server sends to the model, verbatim.

Retire a memory you own (soft-archive — recoverable, not permanently deleted).

USE WHEN: a memory is obsolete/wrong and should stop surfacing in recall.

REQUIRED: memory_id
OPTIONAL: superseded_by_memory_id (records a 'supersedes' link from the newer memory),
          llm_agent_id (caller identity for ownership)

You can only forget memories you created.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `memory_id` | integer | yes | ID of the memory to archive (required) |
| `superseded_by_memory_id` | integer | no | Optional id of the newer memory that replaces this one. |
| `llm_agent_id` | integer | no | Calling agent id (used to verify ownership) |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "memory_id": {
      "type": "integer",
      "description": "ID of the memory to archive (required)"
    },
    "superseded_by_memory_id": {
      "type": "integer",
      "description": "Optional id of the newer memory that replaces this one"
    },
    "llm_agent_id": {
      "type": "integer",
      "description": "Calling agent id (used to verify ownership)"
    }
  },
  "required": [
    "memory_id"
  ]
}
```

Back to the [tool index](../README.md).