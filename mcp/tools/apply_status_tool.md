<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# apply_status_tool

> Apply a tracked status to a record that uses the HasTrackedStatus concern.

| | |
|---|---|
| Category | `data` |
| Callable | MCP clients; in-app @ai conversations (default); grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=apply_status_tool` |

## Description

This is the description the server sends to the model, verbatim.

Apply a tracked status to a record that uses the HasTrackedStatus concern.

Creates a StatusApplication record with timestamps and preserves status history —
NOT a plain attribute update. Use this rather than general_crud_tool for status.

SUPPORTED MODELS: Task, Project, BizTxnEvent, BizTxnAccount, LlmFile, Timesheet

STATUS IDENTIFIERS ARE ENTITY-PREFIXED. A Task takes task_completed, NOT completed.
Both exist as separate TrackedStatusTypes and lookup matches the identifier ALONE, so
passing the unprefixed name silently attaches the wrong status type to the record.
Task: task_not_started, task_in_progress, task_review, task_blocked, task_on_hold,
task_completed, task_cancelled, task_failed, task_backlog.
Other models use their own prefix — list TrackedStatusType via general_crud_tool to
see the real set before guessing.

EXAMPLE: {model_name: "Task", id: 123, status: "task_completed"}

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `model_name` | string | yes | Model name (Task, Project, BizTxnEvent, BizTxnAccount, LlmFile, Timesheet) |
| `id` | integer | yes | Record ID to apply status to. |
| `status` | string | yes | Status internal_identifier — ENTITY-PREFIXED (e.g., task_completed, task_in_progress, task_blocked). An unprefixed name like "completed" matches a separate status type and silently attaches the wrong one. List TrackedStatusType via general_crud_tool if unsure. |
| `start_at` | string | no | Optional start date for the status (YYYY-MM-DD or ISO8601 format). Defaults to current time. |

## Example

```json
{"name": "apply_status_tool", "arguments": {"model_name": "Task", "id": 3717, "status": "task_in_progress"}}
```

Returns the record with its new `current_status`. The change is recorded as a
`StatusApplication` row, so status history is preserved.

## Gotchas

- **Identifiers are entity-prefixed.** A Task takes `task_completed`, not `completed`; the
  unprefixed name matches a different status type and silently attaches the wrong one.
  Task statuses: `task_not_started`, `task_in_progress`, `task_review`, `task_blocked`,
  `task_on_hold`, `task_completed`, `task_cancelled`, `task_failed`, `task_backlog`.
- **Other models use their own prefix.** List `TrackedStatusType` with `general_crud_tool`
  (`{"model_name": "TrackedStatusType", "action": "list", "limit": -1}`) to see the real set.
- **Never update a status through `general_crud_tool`.** It is rejected there by design.

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "model_name": {
      "type": "string",
      "description": "Model name (Task, Project, BizTxnEvent, BizTxnAccount, LlmFile, Timesheet)"
    },
    "id": {
      "type": "integer",
      "description": "Record ID to apply status to"
    },
    "status": {
      "type": "string",
      "description": "Status internal_identifier — ENTITY-PREFIXED (e.g., task_completed, task_in_progress, task_blocked). An unprefixed name like \"completed\" matches a separate status type and silently attaches the wrong one. List TrackedStatusType via general_crud_tool if unsure."
    },
    "start_at": {
      "type": "string",
      "description": "Optional start date for the status (YYYY-MM-DD or ISO8601 format). Defaults to current time."
    }
  },
  "required": [
    "model_name",
    "id",
    "status"
  ]
}
```

Back to the [tool index](../README.md).