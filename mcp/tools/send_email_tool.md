<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# send_email_tool

> Send an email based on a email body, subject, sender and reciever make sure to send all required fields, email_body can not be empty.

| | |
|---|---|
| Category | `communication` |
| Callable | MCP clients; in-app @ai conversations (default); grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=send_email_tool` |

## Description

This is the description the server sends to the model, verbatim.

Send an email based on a email body, subject, sender and reciever make sure to send all required fields, email_body can not be empty. Before calling the tool the email_body should be generated and passed to it.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `subject` | string | yes | Subject for email. |
| `email_body` | string | yes | Body of email should be in HTML and should always be passed. |
| `to_email` | string | yes | To email address, for multiple email addresses they should be separated by ;. |
| `from_email` | string | no | Optional from email address. |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "subject": {
      "type": "string",
      "description": "Subject for email"
    },
    "email_body": {
      "type": "string",
      "description": "Body of email should be in HTML and should always be passed"
    },
    "to_email": {
      "type": "string",
      "description": "To email address, for multiple email addresses they should be separated by ;"
    },
    "from_email": {
      "type": "string",
      "description": "Optional from email address"
    }
  },
  "required": [
    "subject",
    "email_body",
    "to_email"
  ]
}
```

Back to the [tool index](../README.md).