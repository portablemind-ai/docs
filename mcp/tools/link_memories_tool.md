<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# link_memories_tool

> Create a relationship edge between two memories in the same tenant.

| | |
|---|---|
| Category | `memory` |
| Callable | MCP clients; in-app @ai conversations (always on); grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=link_memories_tool` |

## Description

This is the description the server sends to the model, verbatim.

Create a relationship edge between two memories in the same tenant.

REQUIRED: source_memory_id, target_memory_id, relationship_type
OPTIONAL: strength (0.0-1.0, default 0.8)

relationship_type one of: caused_by, depends_on, leads_to, references, spawns, supersedes, validates, related_to, exemplifies

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `source_memory_id` | integer | yes | ID of the source memory (required) |
| `target_memory_id` | integer | yes | ID of the target memory (required) |
| `relationship_type` | string | yes | Type of relationship (required) One of: `caused_by`, `depends_on`, `leads_to`, `references`, `spawns`, `supersedes`, `validates`, `related_to`, `exemplifies`. |
| `strength` | number | no | Edge strength 0.0-1.0 (default 0.8) |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "source_memory_id": {
      "type": "integer",
      "description": "ID of the source memory (required)"
    },
    "target_memory_id": {
      "type": "integer",
      "description": "ID of the target memory (required)"
    },
    "relationship_type": {
      "type": "string",
      "enum": [
        "caused_by",
        "depends_on",
        "leads_to",
        "references",
        "spawns",
        "supersedes",
        "validates",
        "related_to",
        "exemplifies"
      ],
      "description": "Type of relationship (required)"
    },
    "strength": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Edge strength 0.0-1.0 (default 0.8)"
    }
  },
  "required": [
    "source_memory_id",
    "target_memory_id",
    "relationship_type"
  ]
}
```

Back to the [tool index](../README.md).