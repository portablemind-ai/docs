<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# complete_agent_run_tool

> Complete an LLM agent run that was waiting for user feedback.

| | |
|---|---|
| Category | `agents` |
| Callable | MCP clients |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=complete_agent_run_tool` |

## Description

This is the description the server sends to the model, verbatim.

Complete an LLM agent run that was waiting for user feedback. Updates the agent run with completion status and timestamp.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `successful` | boolean | yes | Whether the agent run completed successfully. |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "successful": {
      "type": "boolean",
      "description": "Whether the agent run completed successfully"
    }
  },
  "required": [
    "successful"
  ]
}
```

Back to the [tool index](../README.md).