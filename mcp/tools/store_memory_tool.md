<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# store_memory_tool

> Store persistent memories for future AI sessions.

| | |
|---|---|
| Category | `memory` |
| Callable | MCP clients; in-app @ai conversations (always on); grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=store_memory_tool` |

## Description

This is the description the server sends to the model, verbatim.

Store persistent memories for future AI sessions.

USE WHEN: User requests to remember something, important decisions/preferences emerge, solutions discovered.

MEMORY TYPES: code_change, bug_fix, discussion, learning, derived_insight, fact, preference, decision, insight, context, solution, relationship, goal, execution

IMPORTANCE: 0.9-1.0 (critical), 0.7-0.8 (important), 0.5-0.6 (moderate), 0.3-0.4 (low), 0.1-0.2 (background)

REQUIRED: title, content
OPTIONAL: memory_type (default: fact), importance_score (default: 0.5), tags [], llm_agent_id, llm_conversation_id, llm_mind_id, private, user_id

LENGTH: store as many memories as you need, but keep each one SHORT. A memory is
re-injected verbatim into every future conversation it matches, so a long one is paid
for again on every turn — write the fact and why it matters, not the transcript it
came from. If you are pasting more than a few sentences, store the conclusion and
point at the source instead.

MINDS: Organizational containers grouping memories by project/context. Use llm_mind_id for project-specific memories.

CROSS-TENANT: llm_conversation_id / llm_agent_id may reference a conversation or agent
shared with you from another tenant — resolution falls back to your shared access
automatically when the id is not in your tenant.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `title` | string | yes | Concise title that clearly identifies what is being remembered (required) |
| `content` | string | yes | The fact to remember, stated as briefly as it can be stated and still stand on its own. A short paragraph is almost always enough. Recall injects this verbatim into future conversations, so length here is paid for on every turn that matches — prefer the fact and its "why" over a transcript, and link or reference detail that lives elsewhere rather than copying it in. |
| `memory_type` | string | no | Type of memory being stored. Defaults to "fact" if not specified. One of: `code_change`, `bug_fix`, `discussion`, `learning`, `derived_insight`, `fact`, `preference`, `decision`, `insight`, `context`, `solution`, `relationship`, `goal`, `execution`. |
| `importance_score` | number | no | Importance score from 0.0 to 1.0. Higher scores mean more important. Defaults to 0.5. |
| `tags` | array of string | no | Optional tags to help categorize and retrieve the memory later. |
| `llm_agent_id` | integer | no | Optional: Associate this memory with a specific agent. If not provided, will use conversation context. |
| `llm_conversation_id` | integer | no | Optional: Associate this memory with a specific conversation. If provided, the memory will be linked to this conversation. |
| `llm_mind_id` | integer | no | Optional: Associate with a LlmMind (organizational container) for project-scoped memories. Omit for general memories (uses default mind). |
| `private` | boolean | no | Whether this memory should be private (only accessible to creator or in source conversation) |
| `user_id` | integer | no | User ID for user-private memories (optional, defaults to current user) |

## Example

```json
{"name": "store_memory_tool", "arguments": {"title": "PROD deploys are tagged automatically", "content": "deploy_modular.sh tags every PROD release; set RELEASE_SUMMARY before deploying or nothing posts to release-notes.", "memory_type": "fact", "importance_score": 0.8, "tags": ["deploy", "prod"]}}
```

## Gotchas

- **Keep each memory short.** It is re-injected verbatim into every future conversation it
  matches, so a long memory is paid for again on every turn. Store the conclusion and point at
  the source.
- **Store the fact and why it matters,** not the transcript it came from.
- **Correct, don't duplicate:** update an existing memory with `update_memory_tool`; retire a
  wrong one with `forget_memory_tool`.

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Concise title that clearly identifies what is being remembered (required)"
    },
    "content": {
      "type": "string",
      "description": "The fact to remember, stated as briefly as it can be stated and still stand on its own. A short paragraph is almost always enough. Recall injects this verbatim into future conversations, so length here is paid for on every turn that matches — prefer the fact and its \"why\" over a transcript, and link or reference detail that lives elsewhere rather than copying it in."
    },
    "memory_type": {
      "type": "string",
      "enum": [
        "code_change",
        "bug_fix",
        "discussion",
        "learning",
        "derived_insight",
        "fact",
        "preference",
        "decision",
        "insight",
        "context",
        "solution",
        "relationship",
        "goal",
        "execution"
      ],
      "description": "Type of memory being stored. Defaults to \"fact\" if not specified."
    },
    "importance_score": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Importance score from 0.0 to 1.0. Higher scores mean more important. Defaults to 0.5."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional tags to help categorize and retrieve the memory later"
    },
    "llm_agent_id": {
      "type": "integer",
      "description": "Optional: Associate this memory with a specific agent. If not provided, will use conversation context."
    },
    "llm_conversation_id": {
      "type": "integer",
      "description": "Optional: Associate this memory with a specific conversation. If provided, the memory will be linked to this conversation."
    },
    "llm_mind_id": {
      "type": "integer",
      "description": "Optional: Associate with a LlmMind (organizational container) for project-scoped memories. Omit for general memories (uses default mind)."
    },
    "private": {
      "type": "boolean",
      "description": "Whether this memory should be private (only accessible to creator or in source conversation)"
    },
    "user_id": {
      "type": "integer",
      "description": "User ID for user-private memories (optional, defaults to current user)"
    }
  },
  "required": [
    "title",
    "content"
  ]
}
```

Back to the [tool index](../README.md).