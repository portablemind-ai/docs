<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# external_mappings_by_system_tool

> Get external mappings for entities from a specific external system

| | |
|---|---|
| Category | `mdm` |
| Callable | MCP clients; in-app @ai conversations; grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=external_mappings_by_system_tool` |

## Description

This is the description the server sends to the model, verbatim.

Get external mappings for entities from a specific external system

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `external_system_id` | string | yes | External system ID or internal identifier. |
| `entity_type` | string | no | Filter by entity type (e.g., "Party", "BizTxnEvent") |
| `include_attributes` | boolean | no | Include external attributes in response (default: true) |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "external_system_id": {
      "type": "string",
      "description": "External system ID or internal identifier"
    },
    "entity_type": {
      "type": "string",
      "description": "Filter by entity type (e.g., \"Party\", \"BizTxnEvent\")"
    },
    "include_attributes": {
      "type": "boolean",
      "description": "Include external attributes in response (default: true)"
    }
  },
  "required": [
    "external_system_id"
  ]
}
```

Back to the [tool index](../README.md).