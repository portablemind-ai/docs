<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# llm_cost_stats_tool

> Retrieve comprehensive LLM cost statistics and usage data for a tenant.

| | |
|---|---|
| Category | `data`, `admin` |
| Callable | MCP clients; in-app @ai conversations; grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=llm_cost_stats_tool` |

## Description

This is the description the server sends to the model, verbatim.

Retrieve comprehensive LLM cost statistics and usage data for a tenant. Provides cost breakdowns by daily/weekly/monthly periods, users, models, conversations, message types (conversation vs standalone), and deletion status (active vs deleted messages). Supports optional date filtering to analyze specific time periods.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `tenant_id` | string | yes | Tenant ID or enterprise identifier to gather cost statistics for. |
| `start_date` | string | no | Optional start date for filtering costs (YYYY-MM-DD format). Defaults to appropriate period based on granularity (30 days for daily, 84 days for weekly, 365 days for monthly) |
| `end_date` | string | no | Optional end date for filtering costs (YYYY-MM-DD format). Defaults to today. |
| `include_daily` | boolean | no | Include daily cost breakdown. Default: true. |
| `include_weekly` | boolean | no | Include weekly cost breakdown. Default: false. |
| `include_monthly` | boolean | no | Include monthly cost breakdown. Default: false. |
| `include_totals` | boolean | no | Include total statistics breakdown (by user, model, conversation, message type, deletion status). Default: true. |

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "tenant_id": {
      "type": "string",
      "description": "Tenant ID or enterprise identifier to gather cost statistics for"
    },
    "start_date": {
      "type": "string",
      "description": "Optional start date for filtering costs (YYYY-MM-DD format). Defaults to appropriate period based on granularity (30 days for daily, 84 days for weekly, 365 days for monthly)"
    },
    "end_date": {
      "type": "string",
      "description": "Optional end date for filtering costs (YYYY-MM-DD format). Defaults to today"
    },
    "include_daily": {
      "type": "boolean",
      "description": "Include daily cost breakdown. Default: true"
    },
    "include_weekly": {
      "type": "boolean",
      "description": "Include weekly cost breakdown. Default: false"
    },
    "include_monthly": {
      "type": "boolean",
      "description": "Include monthly cost breakdown. Default: false"
    },
    "include_totals": {
      "type": "boolean",
      "description": "Include total statistics breakdown (by user, model, conversation, message type, deletion status). Default: true"
    }
  },
  "required": [
    "tenant_id"
  ]
}
```

Back to the [tool index](../README.md).