<!-- GENERATED by `rake mcp:docs` in model_api (lib/service/mcp_docs_generator.rb). Do not edit by hand: change the tool class, or its overlay in docs/mcp_reference/. -->

# load_memories_tool

> Load memories to provide context for AI conversations.

| | |
|---|---|
| Category | `memory` |
| Callable | MCP clients; in-app @ai conversations (always on); grantable to agents |
| Filter | `https://www.dsiloed.com/api/v1/mcp?tools=load_memories_tool` |

## Description

This is the description the server sends to the model, verbatim.

Load memories to provide context for AI conversations.

MODES: conversation, agent, search, semantic, recent, important, type, related, all
Auto-selects: search (if query), conversation (if llm_conversation_id), agent (if agent_id), recent (default)

MEMORY TYPES: code_change, bug_fix, discussion, learning, derived_insight, fact, preference, decision, insight, context, solution, relationship, goal, execution

PARAMS: mode, query, agent_id, llm_conversation_id, mind_id (for scoping), memory_types [], memory_id (for related),
        limit (1-100, default 20), min_importance (0.0-1.0), include_related (default true), include_content (default true),
        max_relationships (0-20), relationship_strength_threshold (0.0-1.0), tags []

Returns: memories array with id, title, type, importance, content, relationships, and aggregated stats.

QUERY IS SEMANTIC: matched by meaning, so AND/OR/quotes do nothing. Plain words.
If two differently-worded queries return the same rows, it is not in memory — stop rewording.
RECOLLECTION, NOT A DIRECTORY: ids and current facts come from org_lookup_tool (people),
list_available_agents_tool (agents), general_crud_tool (records).

CROSS-TENANT: llm_conversation_id / agent_id may reference a conversation or agent
shared with you from another tenant — resolution falls back to your shared access
automatically when the id is not in your tenant.

## Parameters

| Name | Type | Required | Description |
|---|---|---|---|
| `mode` | string | no | Retrieval mode determining how to find memories. Use "semantic" for vector similarity search. Defaults to "search" if query provided, "all" otherwise. One of: `conversation`, `agent`, `search`, `semantic`, `recent`, `important`, `type`, `related`, `all`. |
| `query` | string | no | Search query for content-based memory retrieval. Used with "search" mode. |
| `agent_id` | integer | no | Agent ID for retrieving agent-specific memories. Used with "agent" mode. |
| `llm_conversation_id` | integer | no | Conversation ID for retrieving conversation-specific memories. Used with "conversation" mode. |
| `mind_id` | integer | no | Optional: Scope to specific LlmMind (organizational container). Omit for cross-mind search. |
| `memory_types` | array of string | no | Filter by specific memory types. Can be combined with other modes. |
| `memory_id` | integer | no | Specific memory ID to find related memories. Used with "related" mode. |
| `limit` | integer | no | Maximum number of memories to retrieve. Defaults to 20. |
| `min_importance` | number | no | Minimum importance score for filtering memories. Defaults to 0.0 (no filtering). |
| `include_related` | boolean | no | Whether to include related memories through relationships. Defaults to true. |
| `include_content` | boolean | no | Whether to include full memory content in results. Defaults to true. |
| `max_relationships` | integer | no | Maximum number of relationships to include per memory. Defaults to 5. Set to 0 to exclude all relationships. |
| `relationship_strength_threshold` | number | no | Minimum relationship strength to include. Defaults to 0.70. Only relationships with strength >= this value will be included. |
| `tags` | array of string | no | Filter memories by specific tags. |

## Example

```json
{"name": "load_memories_tool", "arguments": {"query": "deploy procedure for production", "limit": 10, "min_importance": 0.5}}
```

Returns matching memories (id, title, type, importance, content, relationships) plus
aggregate stats.

## Gotchas

- **The query is semantic.** AND/OR/quotes do nothing; use plain words. If two differently
  worded queries return the same rows, it is not in memory — stop rewording.
- **Memory is recollection, not a directory.** Ids and current facts come from
  `org_lookup_tool` (people), `list_available_agents_tool` (agents) and `general_crud_tool` (records).
- **Scope with `mind_id`** for project-specific memories; omit it to search across minds. (Agent-specific: `agent_id`.)

## Input schema

The JSON Schema served for `inputSchema`, verbatim.

```json
{
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "enum": [
        "conversation",
        "agent",
        "search",
        "semantic",
        "recent",
        "important",
        "type",
        "related",
        "all"
      ],
      "description": "Retrieval mode determining how to find memories. Use \"semantic\" for vector similarity search. Defaults to \"search\" if query provided, \"all\" otherwise."
    },
    "query": {
      "type": "string",
      "description": "Search query for content-based memory retrieval. Used with \"search\" mode."
    },
    "agent_id": {
      "type": "integer",
      "description": "Agent ID for retrieving agent-specific memories. Used with \"agent\" mode."
    },
    "llm_conversation_id": {
      "type": "integer",
      "description": "Conversation ID for retrieving conversation-specific memories. Used with \"conversation\" mode."
    },
    "mind_id": {
      "type": "integer",
      "description": "Optional: Scope to specific LlmMind (organizational container). Omit for cross-mind search."
    },
    "memory_types": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "code_change",
          "bug_fix",
          "discussion",
          "learning",
          "derived_insight",
          "fact",
          "preference",
          "decision",
          "insight",
          "context",
          "solution",
          "relationship",
          "goal",
          "execution"
        ]
      },
      "description": "Filter by specific memory types. Can be combined with other modes."
    },
    "memory_id": {
      "type": "integer",
      "description": "Specific memory ID to find related memories. Used with \"related\" mode."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "description": "Maximum number of memories to retrieve. Defaults to 20."
    },
    "min_importance": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Minimum importance score for filtering memories. Defaults to 0.0 (no filtering)."
    },
    "include_related": {
      "type": "boolean",
      "description": "Whether to include related memories through relationships. Defaults to true."
    },
    "include_content": {
      "type": "boolean",
      "description": "Whether to include full memory content in results. Defaults to true."
    },
    "max_relationships": {
      "type": "integer",
      "minimum": 0,
      "maximum": 20,
      "description": "Maximum number of relationships to include per memory. Defaults to 5. Set to 0 to exclude all relationships."
    },
    "relationship_strength_threshold": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Minimum relationship strength to include. Defaults to 0.70. Only relationships with strength >= this value will be included."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Filter memories by specific tags"
    }
  },
  "required": []
}
```

Back to the [tool index](../README.md).